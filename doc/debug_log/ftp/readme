FTP客户端与服务器交互时，存在超时无响应的错误
	2013-06-06
	
	ftpClient.connect("xxx");
	ftpClient.login("xxx", "xxx");
	
	如果网络没有打开，会直接报错，抛出异常；
	但是，如果网络可用，而该网络无法链接该站点，会导致connect一直无法返回，进而导致apk超时错误。
	
	解决方法：
		需要在connect之前，加上超时设置
		ftpClient.setConnectTimeout(2000);
